const devBaseURL = "https://api.it120.cc/changan123";
const proBaseURL = "https://api.it120.cc/changan123";
export const BASE_URL = process.env.NODE_ENV === 'development' ? devBaseURL: proBaseURL;

export const TIMEOUT = 5000;
